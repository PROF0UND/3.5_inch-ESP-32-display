// Optional local Arduino config.
// Copy this file to wifi_config.h, edit the values, and keep wifi_config.h out of Git.
// The .env file is useful for storing the same values, but Arduino IDE does not
// automatically load .env files during compile/upload.

#define WIFI_SSID "UMBC Visitor"
#define WIFI_PASSWORD ""

// Browser login for the SD-card file explorer.
#define FILE_SERVER_USERNAME "admin"
#define FILE_SERVER_PASSWORD "esp32"
